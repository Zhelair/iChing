# Server-only values for the /api/feedback Vercel Function.
# Never prefix these with VITE_ and never commit real secrets.
RESEND_API_KEY=
FEEDBACK_TO_EMAIL=
FEEDBACK_FROM_EMAIL="Yi Path <onboarding@resend.dev>"
